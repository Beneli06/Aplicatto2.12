# Example environment variables (DO NOT commit real secrets)
VITE_API_BASE=/api/v1
# API_KEY should live on the server only (not in frontend)
API_KEY=REPLACE_WITH_SERVER_API_KEY
JWT_SECRET=REPLACE_IN_PROD
PORT=3001
DB_HOST=127.0.0.1
DB_USER=root
DB_PASS=changeme
DB_NAME=aplicatto
